import React from 'react';

const Header = ()=>{
    return(
        <header>
        <h1>Weather App</h1>
        <p> Get real time weather info for your location</p>
        </header>
    );
};

export default Header;